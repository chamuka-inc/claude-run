[package]
name = "claude-run-verify"
version.workspace = true
edition.workspace = true
description = "Generic verify-fix loop subcommand for claude-run"

[dependencies]
claude-run-lib = { workspace = true }
tokio = { workspace = true }
